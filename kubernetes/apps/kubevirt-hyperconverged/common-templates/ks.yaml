---
{
  apiVersion: "kustomize.toolkit.fluxcd.io/v1",
  kind: "Kustomization",
  metadata: {
    name: "common-templates",
  },
  spec: {
    interval: "1h",
    path: "./kubernetes/apps/kubevirt-hyperconverged/common-templates/manifests",
    prune: false,
    sourceRef: {
      kind: "GitRepository",
      name: "flux-system",
      namespace: "flux-system",
    },
    dependsOn: [{
      name: "kubevirt-instance",
    }],
  },
}
