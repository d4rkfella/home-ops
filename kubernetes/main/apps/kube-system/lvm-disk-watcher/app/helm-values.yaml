image:
  repository: darkfella/lvm-disk-watcher
  pullPolicy: IfNotPresent
  tag: v0.26.0
configmap:
  disk-config:
    enabled: true
    data:
      disk-config: |
        k8s-0: /dev/sdb /dev/sdc /dev/sdd /dev/sde /dev/sdf /dev/sdg
securityContext:
  container:
    readOnlyRootFilesystem: false
