---
# yaml-language-server: $schema=https://kubernetes-schemas-9iu.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
{
  apiVersion: "kustomize.toolkit.fluxcd.io/v1",
  kind: "Kustomization",
  metadata: {
    name: "kometa-quickstart",
  },
  spec: {
    components: [
      "../../../../components/volsync",
    ],
    dependsOn: [{
      name: "keycloak-instance",
      namespace: "keycloak",
    }],
    path: "./kubernetes/apps/media/kometa-quickstart/manifests",
    postBuild: {
      substitute: {
        APP: "kometa-quickstart",
        VOLSYNC_CAPACITY: "2Gi",
      },
    },
    prune: true,
    sourceRef: {
      kind: "GitRepository",
      name: "flux-system",
      namespace: "flux-system",
    },
    wait: false,
    interval: "1h",
  },
}
