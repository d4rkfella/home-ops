apiVersion: v1
data:
  X-Content-Type-Options: nosniff
  X-Frame-Options: SAMEORIGIN
  Permissions-Policy: "geolocation=(), microphone=(), camera=(), fullscreen=(), payment=(), display-capture=(), sync-xhr=()"
  Referrer-Policy: strict-origin-when-cross-origin
kind: ConfigMap
metadata:
  name: custom-headers
  namespace: network
