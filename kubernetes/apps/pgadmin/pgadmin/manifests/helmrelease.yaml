---
{
  # yaml-language-server: $schema=https://51f35e2d.kubernetes-schemas-9iu.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
  apiVersion: "helm.toolkit.fluxcd.io/v2",
  kind: "HelmRelease",
  metadata: {
    name: "pgadmin",
  },
  spec: {
    interval: "1h",
    chartRef: {
      kind: "OCIRepository",
      name: "pgadmin",
    },
    values: {
      controllers: {
        pgadmin: {
          annotations: {
            reloader.stakater.com/auto: "true",
          },
          containers: {
            pgadmin: {
              image: {
                repository: "ghcr.io/d4rkfella/pgadmin4",
                tag: "9.13@sha256:858f2c1e9a48faf93f16d78f9fb1e8640fdf86fc646a64be87977721ccc20ea0",
              },
              env: {
                GUNICORN_LIMIT_REQUEST_LINE: 0,
                GUNICORN_LIMIT_REQUEST_FIELD_SIZE: 0,
                AUTHENTICATION_SOURCES: "['oauth2']",
                MASTER_PASSWORD: "True",
              },
              envFrom: [{
                secretRef: {
                  name: "pgadmin-secret",
                },
              }],
              securityContext: {
                allowPrivilegeEscalation: false,
                capabilities: {
                  drop: [
                    "ALL",
                  ],
                },
                readOnlyRootFilesystem: true,
              },
            },
          },
        },
      },
      defaultPodOptions: {
        hostUsers: false,
        securityContext: {
          runAsNonRoot: true,
          runAsUser: 5050,
          runAsGroup: 5050,
          fsGroup: 5050,
          fsGroupChangePolicy: "Always",
          seccompProfile: {
            type: "RuntimeDefault",
          },
        },
      },
      persistence: {
        config: {
          existingClaim: "{{ .Release.Name }}",
          globalMounts: [{
            path: "/var/lib/pgadmin",
          }],
        },
        config-distro: {
          type: "configMap",
          name: "pgadmin4-config",
          globalMounts: [{
            path: "/usr/share/pgadmin4/config_distro.py",
            subPath: "config_distro.py",
            readOnly: true,
          }],
        },
        gunicorn-config: {
          type: "configMap",
          name: "pgadmin4-config",
          globalMounts: [{
            path: "/usr/share/pgadmin4/gunicorn_config.py",
            subPath: "gunicorn_config.py",
            readOnly: true,
          }],
        },
        empty: {
          type: "emptyDir",
          sizeLimit: "100Mi",
          globalMounts: [{
            path: "/tmp",
            subPath: "tmp",
          }, {
            path: "/var/log",
            subPath: "log",
          }],
        },
      },
      route: {
        pgadmin: {
          hostnames: [
            "{{ .Release.Name }}.darkfellanetwork.com",
          ],
          parentRefs: [{
            name: "internal",
            namespace: "envoy-gateway-system",
          }],
          rules: [{
            backendRefs: [{
              port: 80,
              name: "pgadmin",
            }],
          }],
        },
      },
      service: {
        pgadmin: {
          controller: "pgadmin",
          ports: {
            http: {
              port: 80,
            },
          },
        },
      },
    },
  },
}
