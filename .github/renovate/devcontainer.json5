{
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update version in Dockerfile",
      "fileMatch": ["(^|/|\\.)Dockerfile$", "(^|/)Dockerfile\\.[^/]*$"],
      "matchStrings": [
        "# renovate: depName=\"(?<depName>.*?)\"",
        "ARG (?<argName>.*?)_VERSION=(?<currentValue>v?\\d+\\.\\d+\\.\\d+)"
      ],
      "autoReplaceStringTemplate": [
        "# renovate: depName=\"{{{depName}}}\"",
        "ARG {{{argName}}}_VERSION={{{newValue}}}"
      ],
      "datasourceTemplate": "github-releases",  // Specify the GitHub releases datasource
      "versioningTemplate": "semver"
    }
  ],
  "packageRules": [
    {
      "addLabels": ["renovate/cli-tools"],
      "automerge": true,
      "automergeType": "branch",
      "matchDatasources": ["github-releases"],  // Ensure the rule matches the GitHub releases datasource
      "matchUpdateTypes": ["minor", "patch"],
      "semanticCommitType": "chore",
      "semanticCommitScope": "cli-tools",
      "commitMessageTopic": "update CLI tool version for {{depName}}",
      "commitMessageExtra": "( {{currentValue}} → {{newValue}} )"
    }
  ],
  "debug": true
}
