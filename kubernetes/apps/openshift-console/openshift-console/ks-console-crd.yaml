---
{
  apiVersion: "kustomize.toolkit.fluxcd.io/v1",
  kind: "Kustomization",
  metadata: {
    name: "openshift-console-crds",
    namespace: "flux-system",
  },
  spec: {
    interval: "1h",
    prune: true,
    sourceRef: {
      kind: "GitRepository",
      name: "openshift-api",
    },
    path: "./console/v1/zz_generated.crd-manifests",
  },
}
---
{
  apiVersion: "kustomize.toolkit.fluxcd.io/v1",
  kind: "Kustomization",
  metadata: {
    name: "openshift-project-crds",
    namespace: "flux-system",
  },
  spec: {
    interval: "1h",
    prune: true,
    sourceRef: {
      kind: "GitRepository",
      name: "openshift-api",
    },
    path: "./project/v1/zz_generated.crd-manifests",
  },
}
---
{
  apiVersion: "kustomize.toolkit.fluxcd.io/v1",
  kind: "Kustomization",
  metadata: {
    name: "openshift-user-crds",
    namespace: "flux-system",
  },
  spec: {
    interval: "1h",
    prune: true,
    sourceRef: {
      kind: "GitRepository",
      name: "openshift-api",
    },
    path: "./user/v1/zz_generated.crd-manifests",
  },
}
---
{
  apiVersion: "kustomize.toolkit.fluxcd.io/v1",
  kind: "Kustomization",
  metadata: {
    name: "openshift-route-crds",
    namespace: "flux-system",
  },
  spec: {
    interval: "1h",
    prune: true,
    sourceRef: {
      kind: "GitRepository",
      name: "openshift-api",
    },
    path: "./route/v1/zz_generated.crd-manifests",
  },
}
---
{
  apiVersion: "source.toolkit.fluxcd.io/v1",
  kind: "GitRepository",
  metadata: {
    name: "openshift-api",
    namespace: "flux-system",
  },
  spec: {
    interval: "1h",
    url: "https://github.com/openshift/api",
    ref: {
      branch: "master",
    },
  },
}
