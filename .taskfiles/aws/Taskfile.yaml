---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: '3'

tasks:
  s3-manager:
    desc: Fully interactive R2 S3 browser + manager (inline)
    silent: true
    cmds:
      - ./.taskfiles/aws/s3_browser.py
