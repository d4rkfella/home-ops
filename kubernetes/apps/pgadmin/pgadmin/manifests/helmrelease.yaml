---
# yaml-language-server: $schema=https://51f35e2d.kubernetes-schemas-9iu.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
{
  apiVersion: "helm.toolkit.fluxcd.io/v2",
  kind: "HelmRelease",
  metadata: {
    name: "pgadmin",
  },
  spec: {
    interval: "1h",
    chartRef: {
      kind: "OCIRepository",
      name: "pgadmin",
    },
    values: {
      controllers: {
        pgadmin: {
          annotations: {
            reloader.stakater.com/auto: "true",
          },
          containers: {
            pgadmin: {
              image: {
                repository: "ghcr.io/d4rkfella/pgadmin4",
                tag: "9.13@sha256:1e979cc65cd12ebec572c4cf19519231e83199d618a72a711b82a21d9237a8e4",
              },
              env: {
                GUNICORN_LIMIT_REQUEST_LINE: 0,
                GUNICORN_LIMIT_REQUEST_FIELD_SIZE: 0,
                PGADMIN_CUSTOM_CONFIG_DISTRO_FILE: "/tmp/config_distro.py",
                PGADMIN_DISABLE_POSTFIX: "true",
                PGADMIN_CONFIG_AUTHENTICATION_SOURCES: "['oauth2']",
                PGADMIN_CONFIG_MASTER_PASSWORD: "True",
              },
              envFrom: [{
                secretRef: {
                  name: "pgadmin-secret",
                },
              }],
              securityContext: {
                allowPrivilegeEscalation: false,
                capabilities: {
                  add: [
                    "NET_BIND_SERVICE",
                  ],
                  drop: [
                    "ALL",
                  ],
                },
                readOnlyRootFilesystem: true,
              },
            },
          },
        },
      },
      defaultPodOptions: {
        hostUsers: false,
        securityContext: {
          runAsNonRoot: true,
          runAsUser: 5050,
          runAsGroup: 5050,
          fsGroup: 5050,
          fsGroupChangePolicy: "OnRootMismatch",
          seccompProfile: {
            type: "RuntimeDefault",
          },
        },
      },
      persistence: {
        config: {
          existingClaim: "{{ .Release.Name }}",
          globalMounts: [{
            path: "/var/lib/pgadmin",
          }],
        },
        empty: {
          type: "emptyDir",
          sizeLimit: "100Mi",
          globalMounts: [{
            path: "/tmp",
            subPath: "tmp",
          }, {
            path: "/var/log",
            subPath: "log",
          }],
        },
      },
      route: {
        pgadmin: {
          hostnames: [
            "{{ .Release.Name }}.darkfellanetwork.com",
          ],
          parentRefs: [{
            name: "internal",
            namespace: "envoy-gateway-system",
          }],
          rules: [{
            backendRefs: [{
              port: 80,
              name: "pgadmin",
            }],
          }],
        },
      },
      service: {
        pgadmin: {
          controller: "pgadmin",
          ports: {
            http: {
              port: 80,
            },
          },
        },
      },
    },
  },
}
