---
{
  apiVersion: "kustomize.toolkit.fluxcd.io/v1",
  kind: "Kustomization",
  metadata: {
    name: "kube-prometheus-stack",
  },
  spec: {
    dependsOn: [{
      name: "vault",
      namespace: "vault",
    }],
    path: "./kubernetes/apps/observability/kube-prometheus-stack/manifests",
    prune: true,
    sourceRef: {
      kind: "GitRepository",
      name: "flux-system",
      namespace: "flux-system",
    },
    wait: false,
    interval: "1h",
  },
}
