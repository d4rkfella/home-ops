---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: apk-repository-runner
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: hashicorp-vault
  target:
    name: apk-repository-runner-secret
  dataFrom:
    - extract:
        key: secrets/apk-repository-runner
