---
# yaml-language-server: $schema=https://kubernetes-schemas-9iu.pages.dev/gateway.networking.k8s.io/httproute_v1beta1.json
{
  apiVersion: "gateway.networking.k8s.io/v1",
  kind: "HTTPRoute",
  metadata: {
    name: "keycloak",
    annotations: {
      gatus.home-operations.com/endpoint: "path: /realms/DarkfellaNET/.well-known/openid-configuration",
    },
  },
  spec: {
    hostnames: [
      "accounts.darkfellanetwork.com",
    ],
    parentRefs: [{
      name: "external",
      namespace: "envoy-gateway-system",
      sectionName: "https",
    }],
    rules: [{
      backendRefs: [{
        name: "keycloak-service",
        namespace: "keycloak",
        port: 8080,
      }],
    }],
  },
}
